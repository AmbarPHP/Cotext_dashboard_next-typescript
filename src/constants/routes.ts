import { Search, Settings } from "@mui/icons-material";

export const ROUTES = {
  main: '/dashboard',
  orders: '/orders',
  customers: '/customers',
  inventory: '/inventory',
  task:'/newtask'
};

/* Search, 
dashboard,
sytem Settings,
page tree */
